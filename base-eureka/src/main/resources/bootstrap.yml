#添加HTTP basic基本验证
security:
  basic:
    enabled: true
  user:
    name: eureka
    password: eureka_132
#只接受以下ip
spring:
  application:
    name: base-eureka
  cloud:
    inetutils:
      preferredNetworks:
        - 192.168

server:
  port: 12001

eureka:
  instance:
    metadata-map:
      user.name: ${security.user.name}
      user.password: ${security.user.password}

